[phases.setup]
nixPkgs = ['mdbook', 'cargo', 'gcc']

[phases.install]
cmds = [
  'cargo install mdbook-i18n --locked',
  'echo "Skipping npm install"'
]

[phases.build]
cmds = ['mdbook build']

[start]
cmd = 'mdbook serve --hostname 0.0.0.0 --port 3000'
